import {maximumSum} from '../src/maximumSum';

describe(maximumSum.name, () => {
    it('Test 1', () => {
        const data = "";
        const response = maximumSum(data);
        expect(response).toBe("");
    });
    
});
